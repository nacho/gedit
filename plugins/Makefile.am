## Process this file with automake to produce Makefile.in

if HAVE_GNOME
spell = spell
endif

SUBDIRS = client cvsdiff diff email encrypt reverse $(spell) prm convert testselect

EXTRA_DIST = plugin-howto
