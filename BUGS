KNOWN:

	* "Replace All" bug (Bug #6140, #4105)

SMALL:
	
	* If you delete with the delete key v.s. the backspace and the
	  undo the cursor does reposition itself o.k.

UNCONFIRMED:
	
	* Deleting a line will sometimes make the next line down
	  dissappear, until you highlight it.

	* There are issues with auto indent and multiple views of a
          document
